version: 4.0.78
steps:
  - prompt: Click on "Pricing."
    commands:
      - command: hover-text
        text: Pricing
        description: link in the header
        action: click
  - prompt: Assert that a pricing page is displayed.
    commands:
      - command: assert
        expect: Everything you need to boost productivity
  - prompt: Interact with the page to see pricing details.
    commands:
      - command: scroll
        direction: down
  - prompt: >-
      Assert that relevant pricing information is visible.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: Book Demo
